# Arbeits-/Scratch-Ordner (reproduzierbar) -> nicht versionieren
samples/
renders/
# lose Test-Dateien im Wurzelordner ignorieren ...
/*.stl
/*.png
# ... ABER models/ wird versioniert (Meshy-Modelle sind nicht reproduzierbar)
!models/
