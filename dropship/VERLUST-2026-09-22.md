# Verlustware im Verkauf — zwei Artikel raus, der Wächter bekommt einen Starter (Gegenprüfung 22.09., ausgeführt 23.09.2026 00:15 UTC)

Befund der Gegenprüfung vom 22.09.: zwei Artikel, bei denen **jeder Verkauf Geld kostet**, standen in
6 Kanälen kaufbar. `automation/verlustbringer.py` (15.09.) hatte keinen Starter, kein Log, und sein
Ledger stand seit dem 15.09. bei 12 Zeilen.

## Die beiden Artikel — live gelesen, gedraftet, rückgelesen

| Produkt | GID | Preis | unitCost (inkl. Fracht) | Netto bei CHF 7 Versanderlös | Mindestpreis Gratisversand (+3.00 Puffer) | vorher | nachher (frischer Query) |
|---|---|---|---|---|---|---|---|
| Kinder-Autositz Portabel 3-12 J. | `gid://shopify/Product/15450303136129` | 14.90 | 25.07 | **−4.43** | 29.90 (= 2,0-fach → «raus») | ACTIVE, 6 Publikationen, Tags u. a. `rueckhol-0907` | **DRAFT, 0 Publikationen**, + `verlust-auto-draft` + `marge-verlust-draft` |
| Atemschutzmasken-Set mit Filtern | `gid://shopify/Product/15496330150273` | 16.90 | 49.32 | **−26.77** | 54.90 (= 3,2-fach → «raus») | ACTIVE, 6 Publikationen, Tags `beauty`, `hautpflege`, `skincare`, `pflege`, `maske` | **DRAFT, 0 Publikationen**, `beauty`/`hautpflege`/`skincare` entfernt (tagsRemove), + beide Verlust-Tags |

Rechnung: `tools/marge_wahrheit.netto(preis, ek, 7.0)` — Gebühr 2,9 % + 0.30, Währung 1,5 %; unitCost enthält die Fracht bereits.
Mutationen: `productUpdate(product:{id,status:DRAFT})` + `tagsAdd` (+ `tagsRemove` beim Maskenset). **Nie** `productUpdate(input:{tags})`.
Keine Produkt-Notiz, nur Tags (Auftrag).

⚠️ Der Autositz trägt `rueckhol-0907`: ein Rückholer hat ihn am 07.09. schon einmal zurück in den Verkauf gestellt.
Die alte Fassung des Wächters hätte ihm nur `verlust-auto-draft` gegeben — einen Tag, den **kein** Rückholer kennt
(`cj_versand_ch_revive.RISIKO` kennt `marge-verlust-draft`, nicht `verlust-auto-draft`). Deshalb tragen alle Verlust-Drafts
jetzt **beide** Tags.

## Was der Wächter jetzt tut (`automation/verlustbringer.py`, neu)

| Vorher (15.09.) | Jetzt (23.09.) |
|---|---|
| scharf war der Standard, `DRY=1` der Ausnahmefall | **Standard = Trockenlauf**; scharf nur mit `--scharf` oder `SCHARF=1` (Aufseher) |
| Export fest `/tmp/marge_export.jsonl` (existierte nicht) | nimmt den jüngsten Export mit `price`+`unitCost` (`/tmp/kost28.jsonl` der täglichen Kosten-Kette); **älter als 3 Tage → `PAUSE: wartet auf Export`**, nichts erfunden |
| `productUpdate(input:{status,tags})` — ersetzt alle Tags | `productUpdate(product:{status})` + `tagsAdd` beider Tags |
| Ledger: nur GID | Ledger je Zeile `gid ⇥ zeit ⇥ was ⇥ preis ⇥ ek ⇥ titel`, `flush()` nach jeder Zeile; alte 12 Zeilen bleiben lesbar |
| Export-Zahlen wurden geschrieben | **vor jeder Mutation live lesen** (Status, Preis, EK, Verlust neu rechnen; EK unbekannt → nicht anfassen), **danach rücklesen** (Status = DRAFT und beide Tags) — erst dann Ledger |
| «heben» setzte Preise (`productVariantsBulkUpdate`) | **«heben» wird nur gemeldet** (`dropship/_verlust_heben.json`, 416 Einträge mit Mindestpreis) — Preisschreiber-Absprache offen |
| kein Stand, keine Ampel | `dropship/_verlust_stand.json` nach jedem Lauf (auch Trockenlauf) → Ampel-Zeile «VERLUST: N kaufbar» |
| Ledger = für immer erledigt | **Rückkehrer**: steht ein Produkt im Ledger, aber im Export aktiv und im Verlust → wieder Kandidat (Zombie-Ledger-Klasse) |
| keine Eimer-Etikette | `eimer_etikette.nachlauf()` nach jeder Antwort |

Reihenfolge scharf: teuerste Verluste (grösster EK-Überhang) zuerst, `CAP` je Lauf (Standard 300). Solange «raus» offen bleibt, endet
der Lauf ohne `FERTIG` → der Aufseher startet ihn beim nächsten Tick weiter; `FERTIG` erst, wenn keine Verlustware mehr im Verkauf steht.

## Gemessen (23.09. 00:15 UTC, Export `/tmp/kost28.jsonl` vom 21.09. 18:59 UTC = 29,3 h alt)

| | Zahl |
|---|---|
| Produkte im Export (status:active am 21.09.) | 51'344 |
| Varianten mit unitCost | 302'712 von 431'185 |
| Verlust, Preis müsste ≤ 1,6-fach steigen («heben», nur Meldung) | **416** |
| Verlust, aus dem Verkauf («raus») | **1'317** (Baseline altes Skript: 1'319; die 2 Hand-Drafts erkennt es als «seit dem Export erledigt») |
| Rückkehrer (Ledger sagt erledigt, Export sagt aktiv) | 0 |
| Scharf-Test `CAP=2 --scharf` | 2 gedraftet und rückgelesen: Faltbarer Aufbewahrungs-Schrank (77.90 / EK 332.73), Deckenventilator mit Beleuchtung (53.90 / EK 307.33) — beide DRAFT, 0 Publikationen, beide Tags (unabhängiger Query) |
| **kaufbar nach diesem Lauf (Export-Stand)** | **1'731** = 416 heben + 1'315 raus-Rest |
| Ledger | 16 Zeilen (12 alt, 2 hand-draft, 2 draft) |

Die 12 Drafts vom 15.09. (Subwoofer, Katzenbäume, Teppich …) trugen **keinen** Verlust-Tag → per `tagsAdd` nachgetragen, 12 von 12 rückgelesen.

## Offen (nicht in meinen Dateien)

1. **Rückholer kennen `verlust-auto-draft` nicht** — `cj_versand_ch_revive.RISIKO` und das Klingen-Tor in `cj_verfuegbarkeit.py`
   (beide werden gerade von einer anderen Session bearbeitet). Übergangsschutz: der zweite Tag `marge-verlust-draft`, den `RISIKO`,
   `social_queue_wache` und `querbeet_kuratieren` schon kennen. `cj_verfuegbarkeit.klingen_tor` fragt nur nach Klingen → dort fehlt jede Marge-Frage.
2. **Preisschreiber-Absprache:** 416 «heben»-Produkte bleiben kaufbar und verlieren bei jedem Verkauf. Wer Preise setzen darf
   (`preisboden`, `reprice_to_benchmark`, `kosten_boden15_korrigieren`), ist nicht abgesprochen — bis dahin nur die Meldeliste.
3. **Kinder-Autositz = Sicherheitsprodukt ohne ECE-R44/R129-Angabe** — auch bei richtigem Preis nicht ohne Nachweis zurück in den Verkauf
   (eigene Klasse, wie Klingen/Heilversprechen; ein Rückholer nach Preis allein reicht nicht).
4. **Atemschutzmasken-Set:** `productType` steht weiterhin auf «Hautpflege» (Feld, kein Tag — Auftrag war «nur Tags»); die Tags `pflege`/`maske` bleiben.
5. Starter im Aufseher + Ampel-Zeile: Schnipsel liegen in der Rückmeldung (Dateien `fixer_keepalive.sh`/`betreiber_ampel.py` nicht angefasst).

## Prüfbefehl

```
TOK=$(cat /tmp/cj_shop_token.txt); curl -s -H "X-Shopify-Access-Token: $TOK" -H "Content-Type: application/json" \
  -d '{"query":"{ a: product(id:\"gid://shopify/Product/15450303136129\"){status tags resourcePublicationsCount{count}} b: product(id:\"gid://shopify/Product/15496330150273\"){status tags resourcePublicationsCount{count}} }"}' \
  https://au3j0y-hq.myshopify.com/admin/api/2026-01/graphql.json
```
Erwartet: beide `DRAFT`, `count 0`, Tags enthalten `verlust-auto-draft` und `marge-verlust-draft`; b ohne `beauty`/`hautpflege`/`skincare`.
