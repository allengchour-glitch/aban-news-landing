/* aban Werbe-Konfiguration (Google AdSense) — eine Quelle der Wahrheit.
   Leer ("") -> es wird KEINE Werbung angezeigt (Slots bleiben leer).
   So aktivierst du Werbung (nur DU):
     1) Google-AdSense-Konto erstellen + Seite zur Prüfung einreichen (abannews.com).
     2) Nach Freigabe deine Publisher-ID hier eintragen: ADSENSE_CLIENT = "ca-pub-XXXXXXXXXXXXXXXX".
     3) Optional je Platzierung eine Ad-Slot-ID in SLOTS hinterlegen (aus dem AdSense-Dashboard).
   Werbung wird IMMER sichtbar als „Anzeige" gekennzeichnet (Pflicht). */
window.ABAN_ADS = {
  ADSENSE_CLIENT: "",            // z. B. "ca-pub-1234567890123456" — leer = aus
  SLOTS: {
    // "marktplatz-top": "1234567890",
    // "kategorie-mid": "0987654321"
  }
};
