// ============================================================
// AFFILIATE-LINKS — zentrale Konfiguration (eine Quelle der Wahrheit)
// Wird von den Review-Seiten geladen (z. B. ki-stimmen.html).
//
// WICHTIG (Markenregeln, CLAUDE.md):
//  - Affiliate-Links NUR auf ehrlichen Review-/Kontextseiten, NIE in der
//    Tool-Datenbank (tools.html) — die bleibt affiliate-frei (kein Bias).
//  - Jeder Link MUSS auf der Seite sichtbar als Affiliate gekennzeichnet sein.
//  - Nur Tools empfehlen, die wirklich getestet wurden.
//
// Pro Tool der PartnerStack-Tracking-Link (get.murf.ai/<id> etc.).
// Leer ("") -> Button/Link zeigt auf die neutrale Anbieter-Domain ohne Tracking
// (kein toter Link, keine Empfehlung ohne Ziel).
// ============================================================
window.ABAN_AFFILIATE = {
  // Murf AI (Text-to-Speech). Tracking via PartnerStack.
  // Tipp: der themenspezifische text-to-speech-Link konvertiert besser als die
  // Startseite — sobald du ihn vollstaendig kopiert hast, hier eintragen:
  //   z.B. "https://get.murf.ai/text-to-speech-6owime........"
  MURF_URL: "https://get.murf.ai/hhd1supey3ea"
};
