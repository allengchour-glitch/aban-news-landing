name: Prompt-Bibliothek — Auto-Build

on:
  push:
    paths:
      - 'prompts-bibliothek/**'
  workflow_dispatch:

jobs:
  build:
    name: Generate prompt library
    runs-on: ubuntu-latest
    timeout-minutes: 5
    # Secret-gated Deploy (wie cloudflare-pages.yml): ohne Token passiert nichts.
    env:
      CF_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CF_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Generate site
        run: |
          cd prompts-bibliothek
          python generate.py
          find dist -type f | wc -l
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: prompts-bibliothek-dist
          path: prompts-bibliothek/dist
          retention-days: 14

      # --- Deploy nach Cloudflare Pages (nur auf main, nur mit Secret) ---
      # Ohne CLOUDFLARE_API_TOKEN werden beide Schritte übersprungen → Check bleibt grün.
      # Mit Secret: legt das Pages-Projekt "prompts" bei Bedarf an und deployt prompts-bibliothek/dist.
      # Danach im CF-Dashboard nur noch die Custom Domain prompts.abannews.com zuordnen.
      - name: Ensure Cloudflare Pages project exists
        if: ${{ github.ref == 'refs/heads/main' && env.CF_API_TOKEN != '' }}
        continue-on-error: true
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ env.CF_API_TOKEN }}
          accountId: ${{ env.CF_ACCOUNT_ID }}
          command: pages project create prompts --production-branch=main
      - name: Deploy to Cloudflare Pages
        if: ${{ github.ref == 'refs/heads/main' && env.CF_API_TOKEN != '' }}
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ env.CF_API_TOKEN }}
          accountId: ${{ env.CF_ACCOUNT_ID }}
          command: pages deploy prompts-bibliothek/dist --project-name=prompts --branch=main
      - name: Hinweis, wenn Secrets fehlen
        if: ${{ github.ref == 'refs/heads/main' && env.CF_API_TOKEN == '' }}
        run: |
          echo "CLOUDFLARE_API_TOKEN nicht gesetzt — Deploy übersprungen (Build ok)."
          echo "Secrets unter Settings → Secrets and variables → Actions setzen,"
          echo "dann läuft dieser Workflow das Projekt 'prompts' selbst an."
