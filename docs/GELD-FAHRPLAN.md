# Geld-Fahrplan — Stand & Reihenfolge

> Alles ist **gebaut und vorbereitet**. Was bei jedem Punkt fehlt, ist **genau ein
> Konto-/Klick-Schritt von Aban** — den Rest macht Claude (Code) oder der Browser-Agent.
> Reihenfolge: **die „anderen" jetzt · Buch in ~1 Woche.**
>
> Ehrliche Grundregel: Jeder Weg bringt **klein + langsam** Geld und braucht **Traffic/Zeit**.
> Kein Punkt ist „reich über Nacht". Aber alles ist echt und passiv-fähig.

---

## JETZT — die „anderen" (parallel, je 1 Schritt von dir)

### 1. Affiliate anschalten (passiv, kein Kontakt) · **10 Min**
- **Du:** bei **einem** Programm anmelden (z. B. Jasper → `jasper.ai/partners`), persönlichen Link holen.
- **Claude:** Link in `…-radar/affiliate.json` eintragen, `_` entfernen, push → Seite verlinkt automatisch (getestet).
- **Doku:** `docs/AFFILIATE-ANMELDEN.md` · **Status:** Maschine fertig, wartet auf 1 Link.

### 2. Daten-Produkt verkaufen (no-contact) · **20 Min**
- **Du:** **Gumroad**-Konto erstellen → Datensatz-Datei hochladen → Preis → Link.
- **Claude:** Buy-Link in `ki-tools-datensatz.html` eintragen (statt Mail-Fallback).
- **Status:** Produkt + Seite fertig, wartet auf Bezahllink.

### 3. Radars live schalten (Traffic-Fundament) · Browser-Agent
- **Du + Browser-Agent:** Cloudflare Pages je Subdomain (du bestätigst OAuth/Domain).
- **Doku:** `docs/CLOUDFLARE-DEPLOY-BROWSER-AGENT.md` · **Status:** einige schon live.

---

## IN ~1 WOCHE — Buch auf KDP
- **Du:** KDP-Konto (Steuer-Interview + Bank) + Upload — Browser-Agent hilft bei den Feldern.
- **Bereit:** eBook + Cover datei-geprüft ✅, Listing-Mappe, Upload-Kit (ZIP), Browser-Auftrag.
- **Doku:** `docs/KDP-LISTING-MAPPE.md` · `docs/KDP-UPLOAD-BROWSER-AGENT.md`
- **Warum eBook zuerst:** 100 % reif, kein Ablehnungs-Risiko; Amazon bringt die Käufer (kein Netzwerk nötig).

---

## Empfohlene Reihenfolge (kleinster Aufwand zuerst)
1. **Affiliate-Anmeldung** (10 Min) → sofort passiv, läuft dann allein.
2. **Gumroad Daten-Produkt** (20 Min) → no-contact-Verkauf.
3. **Buch KDP** (nächste Woche, ~30 Min) → Amazon verkauft ohne Kontakt.

> Was Claude (Code) macht: alles Code-/Datei-seitige (Links eintragen, bauen, prüfen).
> Was nur du kannst: das **eine Konto** je Punkt (Identität/Bank/Vertrag) — nicht delegierbar.
> Status gesamt: **3 Wege bereit, 0 Konten angelegt.** Sobald ein Konto/Link da ist, ziehe ich den Rest durch.
